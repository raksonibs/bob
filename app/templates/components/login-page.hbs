<h2> Login Page </h2>

<form {{action "authenticate" on="submit"}}>
  {{input value=login placeholder="Email"}}
  {{input value=password placeholder="password" type="password"}}
  <button type="submit">Login</button>
</form>
