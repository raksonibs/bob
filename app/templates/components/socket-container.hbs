<p> might need to nest everything in socket container which provides everything to it inside? </p>
{{#if newArticles}}
  <h1>New Articles</h1>
  {{!-- {{debugger}} --}}
  {{#each newMessagesArray as |newArticle|}}
    <p>{{newArticle.attributes.title}}</p>
  {{/each}}
{{/if}}
