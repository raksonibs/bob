<h1>Friends Index</h1>
{{! The context here is the controller}}
<h2>Total friends: {{model.length}}</h2>
<ul>
{{#each model as |friend|}}
<li>

  {{!--  that if we pass an instance of a friend to link-to, then the model hook in the friends show route won’t be called. If we want the hook to be called, instead of doing {{#link-to 'friends.show' friend}}, we’ll have to do {{#link-to 'friends.show' friend.id}}. --}}
  {{!-- {{link-to friend.fullName 'friends.show' friend}} computed link content --}}
  <td>{{link-to friend.fullName "articles" friend}}</td>

  <td><a href="#" {{action "delete" friend}}>Delete</a></td>

</li>
{{/each}} </ul>

